## 1. 系統性版面校正（跨生成頁共通）

- [x] 1.1 設定面板 380 → 400px、雙欄 gap 20 → 16px、面板內距 22 → 24px（對齊 `panel_config` 400 + gap 16 + `panel_results` 686 = 1102）

## 2. 設定區（生成前）對齊設計

- [x] 2.1 步驟 2「商品介紹」下方新增「探索靈感素材」pill ＋「依商品類別給文案風格建議」說明（對齊 `row_insp`）
- [x] 2.2 把「輸出比例」從結果區移到設定區，作為**步驟 3**（生成前選定）；改用設計稿的 2 行式比例卡片（1:1 貼文／16:9 橫式／9:16 限動，60px 高、選取態 `bg $blue-light` + `1.5px $blue-dark-500` 邊框）
- [x] 2.3 修正飼料消耗：由寫死「12 顆飼料」→「5 顆飼料」（對齊設計稿 `txt_5 Tokens`）
- [x] 2.4 生成鈕文案「生成貼文」→「產生貼文」；圖示由 `ti-sparkles`（星星）→ `ti-plus`（加號），對齊設計稿（使用者回報後修正）

## 3. 品牌設定開關元件化

- [x] 3.1 把原本行內的 `label.brand` + 自製 switch，換成共用 `BrandToggle` 元件（對齊 Figma `brand_toggle`：底 `#EFF2FA`、圓角 8、開關 32×18 開啟為 `#2E3567`、標題「套用品牌設定」＋副標「品牌色票・浮水印」＋右側「編輯」）
- [x] 3.2 `BrandToggle` 的「編輯」emit 導向 `/settings`

## 4. 結果區重構（對齊設計 `row_result`）

- [x] 4.1 移除原本的「海報／版位預覽」模式切換與 IG 版位卡（`igcard`）——設計稿結果區為固定的「圖 + 文案」兩欄
- [x] 4.2 左欄 `col_image`：貼圖預覽（比例吃設定區選的輸出比例）＋「換一張圖／下載」文字動作
- [x] 4.3 右欄 `col_copy`：文案卡（`copy` + hashtags）＋「複製文案」（`ChipButton`）＋「重寫文案」文字動作
- [x] 4.4 結果下方補上 `row_note` 說明文字
- 決策：移除 `savePoster`／`savedId`／`view`／`brandName` 等結果模式切換相關程式碼與 `useBrandStore`／`useAssets` 引用，避免留下未使用變數（設計稿結果區無「存入圖庫」與模式切換）

## 5. 驗證

- [x] 5.1 `npm run build`（vue-tsc）通過
- [x] 5.2 已對照 MCP 抓取的精確規格（tier／ratio 卡片、`row_note`、生成鈕 `+` 圖示等）比對修正，使用者於瀏覽器截圖確認
