## 1. 四項指標對齊設計

- [x] 1.1 對齊 Requirement「呈現四項 AI 表現指標」：生成成功率（96.2%・無錯誤完成生成的比例）
- [x] 1.2 對齊 Requirement「採用相關指標僅計圖生圖」：採用率（68.4%・生成結果被存入圖庫或下載的比例，只算圖生圖）
- [x] 1.3 平均重生成次數（1.7 次・每個被採用素材前的平均重試）
- [x] 1.4 每採用素材成本（6.1 Tokens／每個被採用素材），加飼料圖示

## 2. 資料來源說明

- [x] 2.1 對齊 Requirement「標示指標需前端埋點」：補上說明區塊：「這四項指標需前端埋點（生成成功／失敗事件、採用事件…）」

## 3. 版面與驗證

- [x] 3.1 措辭／副標／間距／字級依 Figma Inspect 對齊
- [x] 3.2 `npm run build` 通過、視覺比對
- [x] 3.3 對齊 Requirement「呈現四項 AI 表現指標」：修正 `usage.metrics.comparedLastMonth` 只存在於 `zh-Hant.ts` 的缺漏——`UsageView.vue` 有使用該 key，英文介面會直接顯示原始 key 字串。已於 `en.ts` 補上 `'{delta} vs last month'`。驗證：兩份語系檔 key 數量由 548 / 549 對齊為 549 / 549，差集為空
