## 1. 現況盤點

- [x] 1.1 確認 `GenerateImageView.vue` 目前的實作狀態：按鈕已使用 MV-00/MV-01 建立的共用元件（`PrimaryButton`／`GhostButton`／`ChipButton`），其餘圖示仍使用 `@tabler/icons-webfont`（`i.ti.ti-*`）過渡方案，文案、間距、字級、色碼皆為初版實作，尚未比對設計稿

## 2. 精確數值比對（使用者透過 Figma Inspect 逐一提供，持續進行中，本節會隨進度增補）

- [x] 2.1 「從圖庫選擇參考圖」彈窗（`ImagePickerDialog.vue`，MV-02 的「從圖庫選擇」按鈕會開啟這個共用元件，同時也用在 GenerateVideoView／MarketingPostView／TryOnView／LibraryView 的圖庫選取彈窗）依使用者提供的畫面截圖比對出三項差異並修正：(a) 副標文字從「調閱機器人「日安選物」的素材與生成產物」改成「隸屬機器人「日安選物」的素材與生成產物」；(b) 素材卡片的分類標籤（上傳／物件素材／AI 生成／編輯產物／影片）原本各自有不同底色（藍／紫／黃／綠／粉），設計稿上全部統一成同一種淺黃底色＋深褐字（`#FAEEDA` / `#854F0B`，沿用既有 AI 生成標籤的配色），移除其他四種顏色的 CSS 規則與對應的 `tag--*` class；(c) 素材名稱與標籤原本上下兩行，改成同一行左右排列（名稱靠左、標籤靠右，名稱過長時用 `text-overflow: ellipsis` 截斷，讓標籤永遠有位置顯示）。這是共用元件，修改會同時影響上述五個頁面的圖庫選取彈窗
- [ ] 2.2 （待使用者提供 MV-02 主要內容區域——選擇模型、參考圖上傳、描述輸入、進階設定、生成結果——的截圖／SVG 後補上具體比對項目）

## 3. 驗證

- [ ] 3.1 每項修改後執行 `npm run build`（含 `vue-tsc` 型別檢查），確認通過
- [ ] 3.2 啟動本機開發伺服器，用瀏覽器實際渲染結果（DOM／CSS 斷言、截圖）驗證每一項修改，不只憑程式碼推論
