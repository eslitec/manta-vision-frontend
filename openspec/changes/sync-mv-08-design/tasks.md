## 1. 分段導覽（視覺識別／文案風格／合規與授權）

- [x] 1.1 對齊 Requirement「品牌設定分段呈現」：依設計稿把品牌設定分成區塊／分頁導覽（對齊 MV-08 / 08b / 08c / 08d）

## 2. 既有區塊對齊

- [x] 2.1 品牌基本資料（名稱／一句話定位／官網／產業別）欄位與版面對齊設計稿
- [x] 2.2 對齊 Requirement「視覺識別可上傳 Logo 並管理色票」：視覺識別（Logo 上傳 + 色票管理）對齊 MV-08b
- [x] 2.3 對齊 Requirement「文案風格可設定語氣與用語」：文案風格（語氣、常用 hashtag、稱呼、避免字詞）對齊 MV-08c

## 3. 合規與授權（MV-08d，新區塊）

- [x] 3.1 對齊 Requirement「提供合規與授權資訊」：新增「合規與授權」區塊：依設計稿定義內容（肖像權同意條款、圖片授權／使用聲明）
- [x] 3.2 若需新欄位，擴充 `BrandProfile` 型別與 mock（當時範圍下判定不需新增欄位；2026-08-21 比對發現色票占比與「全部套用」可能需要新欄位，該情境另立任務 5.7 重新評估，此項維持完成不回退，以保留當時的判斷紀錄）

## 4. 驗證

- [x] 4.1 間距／字級／色碼依 Figma Inspect 對齊
- [x] 4.2 `npm run build` 通過、視覺比對
- [x] 4.3 共用 `AppTab`／`AppButton`／`IconChevronDown`，並驗證 320、375、768、1024、1440px 無整頁水平溢位
- [x] 4.4 1366 × 940 實測四個卡片為 1102 × 389、1102 × 336、1102 × 440、1102 × 339px

## 5. 2026-08-21 全頁比對後補齊（見 design.md 同日章節）

- [x] 5.1 對齊 Requirement「品牌設定分段呈現」：品牌基本資料四個欄位補上必填／選填標記——品牌名稱、一句話定位、產業別加紅色星號（14px / 500 / `#ff6148`），官方網站加「選填」（11px / 500 / `#b4b9c4`），欄名與標記以 gap 4px 的 `label_wrap` 包裝，字數計數器維持靠右；星號加 `aria-hidden` 並補 `.visuallyHidden` 的「必填」供螢幕閱讀器朗讀。已確認視覺識別／文案風格／合規與授權三頁的 label 在設計稿中皆無標記，不需比照
- [x] 5.2 產業別由原生 `<select>` 改為自訂 listbox，還原 `dropdown_產業別`（node `1139:716`）：頂部搜尋框（392x32）、分組標頭、選項列高 30、選中項右側 `ic_ok` 打勾（15x15）、底部 22px 漸層遮罩與「找不到適合的？選「其他」」提示；實作方式沿用 `ImageEditorWorkspace.vue` 的字型選單。驗證：鍵盤可操作（`role="listbox"` / `role="option"` / `aria-selected`），點擊外部與 Esc 可關閉
- [x] 5.3 產業別選項由 4 個補齊為設計稿的 22 個、分五組（零售與電商 8／餐飲與食品 3／醫療與健康 3／服務業 5／其他 3），並補上對應的 `zh-Hant` 與 `en` 語系字串。驗證：兩份語系檔 key 結構一致
- [x] 5.4 品牌 Logo 區補上 `r2` 的三個文字動作：「重新偵測」「更換 Logo」「移除」，對齊設計稿 `upload_logo` 的 `detect_col`。驗證：三個動作各自可觸發且不影響既有上傳流程
- [x] 5.5 品牌 Logo 區補上偵測成功的正面回饋：`ic_ok` 打勾（14x14）＋「已自動偵測主色」，與既有的 `analyzing`／`paletteError` 併為三種狀態。驗證：上傳成功、辨識中、失敗三種狀態各自顯示正確
- [x] 5.6 萃取色補上像素占比百分比，並在已被指派的色塊下方標出角色，對齊設計稿 `row_extracted`（色塊 52x40、占比示例 62%／18%／11%／6%／3%）。**更正**：初次比對記為「前兩色有用途 note」，實際節點文字是 `已設為主色`／`已設為點綴色`（9px / Medium / `#54c14f`），是「已指派」的狀態標示而非建議用途，因此由 `profile.colors` 即時推導、不需儲存。驗證：占比取自 `extractColors` 的實際回傳，非寫死數字
- [x] 5.7 重新評估 `BrandProfile` 型別與 mock 是否需要新欄位以支援占比與「全部套用」：先確認 `src/utils/colors.ts` 的 `extractColors` 是否回傳像素比例，若否則擴充回傳值，並連動 `BrandProfile` 與 mock。此項取代任務 3.2 在新範圍下的判斷
- [x] 5.8 品牌色票區補上「全部套用」動作，對齊設計稿 `row_sw_head` 右側。驗證：點擊後主色／輔色／點綴色依占比順序一次套用
- [x] 5.9 「避免使用的字詞」與「肖像權同意條款模板」兩個 textarea 的寬度——查核後確認**原比對結論有誤**：實作的 `.field textarea` 早就是 `width: 12.5rem`（200px），高度也已是 5rem／5.5rem（80／88px），與設計稿完全一致，僅 `$bp-md` 以下改為滿版。無需改動、也無需向設計師確認；錯誤結論已在 design.md 留存更正紀錄
- [ ] 5.10 `npm run build`、`npm test -- --run` 通過，並以 1366x940 重新截圖比對四個分頁
- [x] 5.11 修好共用元件 `AppSearchbar.vue` 自建立起就漏掉的 `IconSearch` 元素（`import` 與 `.appSearchbar__icon` 樣式都在、`input` 也保留 `padding-left: 2.5rem` 的空位，但 template 沒有渲染圖示）。驗證：`LibraryView` 與 `ImagePickerDialog` 的搜尋框同步出現放大鏡，且左內距不再是空白
- [x] 5.12 `colors.spec.ts` 補三個測試涵蓋新的占比契約：占比對應像素數且加總為 1、相近色併入代表色不另立一項、`dominantColors` 與 `dominantColorShares` 的色碼順序一致。驗證：既有 5 個測試全部不動也全部通過
- [ ] 5.13 向設計師確認 `tip` 第二行「偵測結果依 Logo 像素占比排序」的措辭：實作的排序是「面積開根號 x 飽和度權重 x 明度權重」的評分，面積是主項但不是唯一依據（既有測試「飽和度加權：小面積的鮮豔色勝過大面積的淡色背景」明確鎖住這個行為）。要嘛微調文案、要嘛改成純占比排序並調整該測試，兩者都不該由前端單方面決定
- [x] 5.14 以 Figma `get_metadata` 取得四個分頁每個節點的實際座標與尺寸，逐格驗算間距後修正兩處：`page_head` 標題與副標的間距 2px 應為 4px、副標行高 19px 應為 17px（29 + 4 + 17 = 50，與設計稿的 1102x50 相符）；萃取色右側 `tip` 兩行文字誤設 `text-align: right`，設計稿中兩個 text 的 x 皆為 0，應靠左。驗證：Logo 區 83px、`row_extracted` 110px、`row_swatch` 92px、色票區 260px、視覺識別整段 489px、合規與授權整段 339px、文案風格整段 440px 皆與設計稿逐項相符
