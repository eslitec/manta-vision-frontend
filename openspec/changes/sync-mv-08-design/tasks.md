## 1. 分段導覽（視覺識別／文案風格／合規與授權）

- [x] 1.1 對齊 Requirement「品牌設定分段呈現」：依設計稿把品牌設定分成區塊／分頁導覽（對齊 MV-08 / 08b / 08c / 08d）

## 2. 既有區塊對齊

- [x] 2.1 品牌基本資料（名稱／一句話定位／官網／產業別）欄位與版面對齊設計稿
- [x] 2.2 對齊 Requirement「視覺識別可上傳 Logo 並管理色票」：視覺識別（Logo 上傳 + 色票管理）對齊 MV-08b
- [x] 2.3 對齊 Requirement「文案風格可設定語氣與用語」：文案風格（語氣、常用 hashtag、稱呼、避免字詞）對齊 MV-08c

## 3. 合規與授權（MV-08d，新區塊）

- [x] 3.1 對齊 Requirement「提供合規與授權資訊」：新增「合規與授權」區塊：依設計稿定義內容（肖像權同意條款、圖片授權／使用聲明）
- [x] 3.2 若需新欄位，擴充 `BrandProfile` 型別與 mock（當時範圍下判定不需新增欄位；2026-08-21 比對發現色票占比與「全部套用」可能需要新欄位，該情境另立任務 5.7 重新評估，此項維持完成不回退，以保留當時的判斷紀錄）

## 4. 驗證

- [x] 4.1 間距／字級／色碼依 Figma Inspect 對齊
- [x] 4.2 `npm run build` 通過、視覺比對
- [x] 4.3 共用 `AppTab`／`AppButton`／`IconChevronDown`，並驗證 320、375、768、1024、1440px 無整頁水平溢位
- [x] 4.4 1366 × 940 實測四個卡片為 1102 × 389、1102 × 336、1102 × 440、1102 × 339px

## 5. 2026-08-21 全頁比對後補齊（見 design.md 同日章節）

- [x] 5.1 對齊 Requirement「品牌設定分段呈現」：品牌基本資料四個欄位補上必填／選填標記——品牌名稱、一句話定位、產業別加紅色星號（14px / 500 / `#ff6148`），官方網站加「選填」（11px / 500 / `#b4b9c4`），欄名與標記以 gap 4px 的 `label_wrap` 包裝，字數計數器維持靠右；星號加 `aria-hidden` 並補 `.visuallyHidden` 的「必填」供螢幕閱讀器朗讀。已確認視覺識別／文案風格／合規與授權三頁的 label 在設計稿中皆無標記，不需比照
- [ ] 5.2 產業別由原生 `<select>` 改為自訂 listbox，還原 `dropdown_產業別`（node `1139:716`）：頂部搜尋框（392x32）、分組標頭、選項列高 30、選中項右側 `ic_ok` 打勾（15x15）、底部 22px 漸層遮罩與「找不到適合的？選「其他」」提示；實作方式沿用 `ImageEditorWorkspace.vue` 的字型選單。驗證：鍵盤可操作（`role="listbox"` / `role="option"` / `aria-selected`），點擊外部與 Esc 可關閉
- [ ] 5.3 產業別選項由 4 個補齊為設計稿的 22 個、分五組（零售與電商 8／餐飲與食品 3／醫療與健康 3／服務業 5／其他 3），並補上對應的 `zh-Hant` 與 `en` 語系字串。驗證：兩份語系檔 key 結構一致
- [ ] 5.4 品牌 Logo 區補上 `r2` 的三個文字動作：「重新偵測」「更換 Logo」「移除」，對齊設計稿 `upload_logo` 的 `detect_col`。驗證：三個動作各自可觸發且不影響既有上傳流程
- [ ] 5.5 品牌 Logo 區補上偵測成功的正面回饋：`ic_ok` 打勾（14x14）＋「已自動偵測主色」，與既有的 `analyzing`／`paletteError` 併為三種狀態。驗證：上傳成功、辨識中、失敗三種狀態各自顯示正確
- [ ] 5.6 萃取色補上像素占比百分比與前兩色的用途註記，對齊設計稿 `row_extracted`（色塊 52x40、占比示例 62%／18%／11%／6%／3%）。驗證：占比總和與 `extractColors` 的回傳一致，非寫死數字
- [ ] 5.7 重新評估 `BrandProfile` 型別與 mock 是否需要新欄位以支援占比與「全部套用」：先確認 `src/utils/colors.ts` 的 `extractColors` 是否回傳像素比例，若否則擴充回傳值，並連動 `BrandProfile` 與 mock。此項取代任務 3.2 在新範圍下的判斷
- [ ] 5.8 品牌色票區補上「全部套用」動作，對齊設計稿 `row_sw_head` 右側。驗證：點擊後主色／輔色／點綴色依占比順序一次套用
- [ ] 5.9 向設計師確認「避免使用的字詞」與「肖像權同意條款模板」兩個 textarea 在設計稿中的 200px 寬是否為刻意設定；在確認前維持 `width: 100%`，不依 200px 實作
- [ ] 5.10 `npm run build`、`npm test -- --run` 通過，並以 1366x940 重新截圖比對四個分頁
