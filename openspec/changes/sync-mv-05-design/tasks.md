## 1. 系統性版面校正

- [x] 1.1 設定面板 380 → 400px、雙欄 gap 20 → 16px、面板內距 22 → 24px

## 2. 品牌設定開關

- [x] 2.1 在「選擇服飾素材」下方新增「套用品牌設定」開關（共用 `BrandToggle`）；「編輯」導向 `/settings`

## 3. 飼料消耗

- [x] 3.1 修正預估消耗：寫死「15 顆飼料」→「12 顆飼料」（對齊設計稿 `txt_12 Tokens`）

## 4. 結果區動作

- [x] 4.1 生成完成後補上結果動作列：「存入圖庫」（`OutlineButton`，存入後顯示「已存入」）、「下載」、「重新生成」
- [x] 4.2 「存入圖庫」接 `useAssets().saveGenerated`；「重新生成」接既有 `onGenerate`；重新生成時重置 `savedId`

## 5. 上傳模特照（功能缺失修正）

- [x] 5.1 「上傳模特照」分頁補上真正的 `<input type="file" accept="image/*">` 上傳區——原本只切換標籤、沒有檔案選擇器，點了不會開本地選圖
- [x] 5.2 選取真人照片後顯示檔名；若尚未完成肖像同意，自動跳出肖像同意視窗（沿用 `consentStore`）

## 6. 驗證

- [x] 6.1 `npm run build`（vue-tsc）通過
- [x] 6.2 已對照 MCP 抓取的精確規格比對修正（含上傳模特照功能修正），使用者於瀏覽器截圖確認
