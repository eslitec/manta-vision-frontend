## 1. 系統性版面校正

- [x] 1.1 設定面板 380 → 400px、雙欄 gap 20 → 16px、面板內距 22 → 24px

## 2. 品牌設定開關

- [x] 2.1 對齊 Requirement「顯示品牌設定開關與飼料消耗」：在「選擇服飾素材」下方新增「套用品牌設定」開關（共用 `BrandToggle`）；「編輯」導向 `/settings`

## 3. 飼料消耗

- [x] 3.1 修正預估消耗：寫死「15 顆飼料」→「12 顆飼料」（對齊設計稿 `txt_12 Tokens`）

## 4. 結果區動作

- [x] 4.1 對齊 Requirement「結果可存入圖庫／下載／重新生成」：生成完成後補上結果動作列：「存入圖庫」（`OutlineButton`，存入後顯示「已存入」）、「下載」、「重新生成」
- [x] 4.2 「存入圖庫」接 `useAssets().saveGenerated`；「重新生成」接既有 `onGenerate`；重新生成時重置 `savedId`

## 5. 上傳模特照（功能缺失修正）

- [x] 5.1 對齊 Requirement「模特可用內建或上傳真人照」：「上傳模特照」分頁補上真正的 `<input type="file" accept="image/*">` 上傳區——原本只切換標籤、沒有檔案選擇器，點了不會開本地選圖
- [x] 5.2 對齊 Requirement「生成前需完成肖像同意」：選取真人照片後顯示檔名；若尚未完成肖像同意，自動跳出肖像同意視窗（沿用 `consentStore`）

## 6. 驗證

- [x] 6.1 `npm run build`（vue-tsc）通過
- [x] 6.2 已對照 MCP 抓取的精確規格比對修正（含上傳模特照功能修正），使用者於瀏覽器截圖確認

## 7. 2026-08-10 狀態複核

- [x] 7.1 「套用品牌設定」初始值改為關閉，使用者仍可自行開啟
- [x] 7.2 對齊 Requirement「首次進入與生成完成狀態分離」：保留首次進入空預覽；完成生成後才顯示「存入圖庫／下載／重新生成」
- [x] 7.3 以 1366×940 驗證：品牌設定預設關閉、首次進入空預覽且無整頁捲軸；肖像同意視窗 560×429 並完整置中；完成生成後顯示結果動作
