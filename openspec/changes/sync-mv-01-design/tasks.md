## 1. 現況盤點

- [x] 1.1 確認 `LibraryView.vue` 目前的實作狀態：文案、間距、字級、色碼皆為初版實作，圖示仍使用 `@tabler/icons-webfont`（`i.ti.ti-*`）過渡方案，尚未比對設計稿

## 2. 精確數值比對（使用者透過 Figma Inspect 逐一提供，持續進行中，本節會隨進度增補）

- [ ] 2.1 （等待使用者提供第一批 Figma Inspect 數值／截圖）

## 3. 資料夾結構重構（使用者提供完整畫面截圖後發現的結構性差異，範圍擴大到功能而非純視覺）

- [x] 3.1 左側資料夾清單重構：從單一扁平清單，改成「全部素材」（含總數）／「系統分類」（依 `AssetTag` 分類：物件素材／AI 生成／編輯產物／影片，各自帶數量）／「我的資料夾」（使用者自訂資料夾，帶數量、右上角 `+` 新增）三段式結構；新增 `src/types/asset.ts` 的 `CATEGORY_TAGS` 常數定義系統分類清單
- [x] 3.2 三段篩選（全部／系統分類／資料夾）改成同一組互斥單選狀態 `activeView`（取代原本只有 `activeFolder`），與「來源」篩選、關鍵字搜尋維持各自獨立、可疊加
- [x] 3.3 頂部提示文字改成依 `activeView` 動態切換：檢視「全部素材」／系統分類時顯示機器人情境說明；檢視某個資料夾時改顯示該資料夾的名稱、數量與「移出後素材仍保留在圖庫」的說明
- [x] 3.4 資料夾清單下方補上「可將素材拖曳至資料夾，或用批次選擇移動」提示文字（純文字說明，實際拖曳互動不在這次範圍內）

## 4. 批次選取與批次操作

- [x] 4.1 每張素材卡片加上 checkbox；選取任一素材後，格線上方顯示批次操作列（已選 N 筆／全選本頁／清除，右側：移至資料夾／移出資料夾［僅在檢視某個資料夾時顯示］／下載／刪除）
- [x] 4.2 「移至資料夾」改成完整彈窗：列出所有資料夾（含數量）供單選、可在彈窗內就地輸入名稱建立新資料夾，確認後套用到所有選取的素材；沿用既有的 `addToFolder`，不需要新的後端方法
- [x] 4.3 「移出資料夾」：只在檢視某個資料夾時顯示，把選取的素材從目前資料夾移除（保留在圖庫、保留其他歸屬）；新增 `removeFromFolder` 方法（`useAssets` composable ＋ `src/api/mock.ts`）
- [x] 4.4 「刪除」改成完整確認彈窗：顯示選取素材的縮圖預覽、「我了解此操作無法復原」勾選框（勾選後才能點擊）、按鈕文字帶筆數；新增 `deleteAssets` 方法（`useAssets` composable ＋ `mock.ts` 的 `deleteImages`）。設計稿上「其中 N 筆已被生成結果引用」的警示先不做——`Asset`／`GeneratedImage` 資料模型目前完全沒有記錄素材與生成紀錄的關聯，沒有真實資料可以判斷，等這層關聯資料真的存在再補
- [x] 4.5 「下載」目前只做畫面呈現（按鈕存在、可點擊），點擊後無實際動作——`Asset` 型別沒有真實檔案 URL，等後端提供真實檔案來源後再接上真正的下載行為

## 5. 分頁

- [x] 5.1 素材格線改成純前端分頁（8 筆一頁），底部顯示「共 N 筆素材」與頁碼列（含省略號，例如 `1 2 3 … 16`）；切換篩選條件／來源／關鍵字時自動跳回第 1 頁

## 6. 非同步生成進度（待處理，尚未實作）

- [ ] 6.1 圖庫格線顯示非同步生成中的項目（例如影片生成），帶進度百分比、剩餘時間估算；完成後自動入庫。這跟頂部工具列「任務」按鈕的數字徽章是同一組任務狀態，兩者要共用同一份資料來源，範圍會跨到 `DefaultLayout.vue`（`home-workbench-ui` capability），需要另外規劃

## 7. 驗證

- [ ] 7.1 每項修改後執行 `npm run build`（含 `vue-tsc` 型別檢查），確認通過
- [ ] 7.2 啟動本機開發伺服器，用瀏覽器實際渲染結果（DOM／CSS 斷言、截圖）驗證每一項修改，不只憑程式碼推論
- [x] 7.3 用 Playwright 走完整批次選取流程驗證：勾選素材 → 開啟移至資料夾彈窗 → 選資料夾 → 確認 → 資料夾數量正確更新；勾選素材 → 開啟刪除確認彈窗 → 勾選才能點刪除 → 確認 → 總數與分頁筆數正確更新
