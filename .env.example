# 複製成 .env（或 .env.local）後修改。Vite 只會把 VITE_ 開頭的變數送進前端程式碼。

# 要不要走真後端。
# - 不設定／設成任何其他值：整站用 src/api/mock.ts 的假資料（預設）
# - 設成字串 'false'：身分驗證走真後端，其餘尚未實作的端點仍然是假資料
#
# 名字讀起來有點繞（USE_MOCK=false 才是打真後端），但它跟現有的
# 「api/index.ts 預設指向 mock」是同一個意思：**沒設定就是假資料**。
# 預設保守是刻意的——忘了設定會看到假資料（很快就發現），
# 總比忘了設定就整站連不上（要查很久）好。
VITE_USE_MOCK=false

# 後端 API 的位址。留空／不設定就好，走預設值 '/api'：
# - 本機開發：vite.config.ts 的 server.proxy 會把 /api 轉給本機後端（docker
#   compose 的 8000 埠），瀏覽器全程只看到 :5173 這一個來源
# - 正式環境：nginx 把前後端擺在同一個網域下，一樣是同一個來源
#
# 前後端定案走「同網域 + nginx 反向代理」，後端不會開 CORS。如果瀏覽器跳出
# 跨網域被擋的錯誤，代表本機 proxy 設定跟後端埠號對不上，不是後端要加白名單。
# 只有在不透過 proxy、直接打後端網址的情況下才需要設這個值：
# VITE_API_BASE=http://localhost:8000
