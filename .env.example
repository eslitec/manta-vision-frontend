# 複製成 .env（或 .env.local）後修改。Vite 只會把 VITE_ 開頭的變數送進前端程式碼。

# 要不要走真後端。
# - 不設定／設成任何其他值：整站用 src/api/mock.ts 的假資料（預設）
# - 設成字串 'false'：身分驗證走真後端，其餘尚未實作的端點仍然是假資料
#
# 名字讀起來有點繞（USE_MOCK=false 才是打真後端），但它跟現有的
# 「api/index.ts 預設指向 mock」是同一個意思：**沒設定就是假資料**。
# 預設保守是刻意的——忘了設定會看到假資料（很快就發現），
# 總比忘了設定就整站連不上（要查很久）好。
VITE_USE_MOCK=false

# 後端 API 的位址。
# - 本機開發：後端 docker compose 起在 8000 埠
# - 留空／不設定：走 '/api'（同源部署，或自行在 vite.config.ts 設 dev proxy）
#
# 後端需要把這個前端的來源加進它的 CORS_ORIGINS，否則瀏覽器會在 preflight
# 就擋掉每一支 API（後端 .env.example 已預設允許 localhost:5173）。
VITE_API_BASE=http://localhost:8000
